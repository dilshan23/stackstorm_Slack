---
  name: "send_message"
  runner_type: "python-script"
  description: "send a message to a channel"
  enabled: true
  entry_point: "send_message_to_slack.py"
  parameters:
    text:
      type: "string"
      description: "message to send"
      required: false
    whatsapp_status:
      type: "string"
      description: "whatsapp_status"
      required: false